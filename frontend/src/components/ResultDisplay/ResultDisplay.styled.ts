import styled from 'styled-components';

export const ResultDisplayWrapper = styled.div`
    display: block;
    width: 100%;
    border-radius: 10px;
    border: solid 1px var(--borderColor);
    padding: 10px;
    margin-top: 20px;
    background-color: var(--foreground);
    color: var(--background);
`;
